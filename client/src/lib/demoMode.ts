// This file is intentionally kept empty after removing demo mode functionality
// as requested by the user.

export const isInDemoMode = (): boolean => {
  return false; // Always return false - demo mode is completely disabled
};